# 
# DefKorns Easy Custom Borders - v1.2 Mar 27, 2018
#
# Revisioned and improved by DanTheMan827
# Special thanks to KMFDManic for putting up with me and u/Pretendtious for testing
transfer_default
mkdir -p "$mountpoint/var/lib/clover/backgrounds"
copy "$rootfs/backgrounds" "$mountpoint/var/lib/clover"
rm -fr "$rootfs/backgrounds"

cfg_DefKorns_bordershack_enabled='y'
return 1
